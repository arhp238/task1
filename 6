#include <iostream>

int main() {
    int days;
    std::cout << "введите кол-во дней: ";
    std::cin >> days;

    int weeks = days / 7;
    int remaining_days = days % 7;

    std::cout << weeks << " недель и " << remaining_days << " дней" << std::endl;

    return 0;
}
